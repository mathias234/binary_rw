[package]
name = "binary_rw"
version = "2.0.1"
authors = ["Mathias Danielsen <mathiasda98@hotmail.com>"]
edition = "2021"

description = "A binary reader/writer library for rust"
repository = "https://github.com/mathias234/binary-rs"
readme = "README.md"
keywords = ["binary", "reader", "writer"]
license = "MIT"

[features]
wasm32 = []
serde = ["serde_core"]

[dependencies]
thiserror = "1"
serde_core = { version = "1", optional = true, package = "serde" }

[dev-dependencies]
anyhow = "1"
